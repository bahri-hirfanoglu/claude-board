services:
  claude-board:
    build: .
    ports:
      - "${PORT:-4000}:4000"
    volumes:
      - claude-data:/app/data
    environment:
      - PORT=4000
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  claude-data:
